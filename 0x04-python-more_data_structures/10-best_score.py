#!/usr/bin/python3
def multiply_list_map(my_list=[], number=0):
	"""This function that returns a list with all 
	values multiplied by a number without using any loops."""
	if not a_dictionary:
		return (None)
	return (max(a_dictionary, key=a_dictionary.get))
